# ExamForge Environment Configuration

# FastAPI Backend URL (Used for API calls and proxy rewrites)
# For local development: http://localhost:8000
# For production (Render / Railway / Cloud): https://your-backend-api.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Next.js Node Environment
NODE_ENV=production
